import UIKit

protocol ThemeProtocol {
    var unselectedItemTintColor: UIColor  { get }
    var backgroundColor: UIColor { get }
    var labelColor: UIColor { get }
    var textFieldsBackgrounds: UIColor { get }
    var textFieldsPlaceholder: UIColor { get }
    var navigationBackground: UIColor { get }
    var itemTintColor: UIColor { get }
    
}
